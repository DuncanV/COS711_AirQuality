#imports

# inherit
from Network import Network


class CNN(Network):
    def __int__(self, inputShape, outputShape, _learningRate=0.1, _epoches=10, _batchSize=128):
        print('in CNN')
        super().__int__(_model=[], _name="ResNet-50")
        return
